import SectionTitle from "../tittle_section";
import SkillsContainer from "./container_skills";

export default function MainSkills() {
    return (
        <div className="bg-gradient-to-r from-blue-950 to-black">
            <SectionTitle title="SKILLS"/>
            <SkillsContainer/>
        </div>
    )
}